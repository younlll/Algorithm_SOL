#include <iostream>
#include <vector>

using namespace std;

struct Node {
	int left;
	int right;
	int col;
};

vector<Node> vc[10001];
int N;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	cin >> N;

	int a, b, c;
	for (int i = 0; i < N; i++) {
		cin >> a >> b >> c;
		
	}
}